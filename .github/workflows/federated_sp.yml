name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: workflow_dispatch

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: 'Update git folder'
    environment: prod
    env:
      DATABRICKS_AUTH_TYPE: github-oidc
      DATABRICKS_HOST: https://adb-2531935507212601.1.azuredatabricks.net
      DATABRICKS_CLIENT_ID: b7d35f20-a3a7-4a44-a515-9c81d999d362

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Databricks CLI
        uses: databricks/setup-cli@main

      - name: Test Databricks authentication
        run: databricks current-user me

      # - name: Test Databricks directory
      #   run: pwd

      # - name: Clone or update git repository
      #   run: |
      #     REPO_PATH="/Workspace/Users/cinyoung.hur@databricks.com/test_cicd_01"
      #     REPO_URL="https://github.com/${{ github.repository }}"
          
      #     # Try to update existing repo first
      #     if databricks repos update "$REPO_PATH" --branch main 2>/dev/null; then
      #       echo "✅ Repository updated successfully"
      #     else
      #       echo "Repository doesn't exist, attempting to clone..."
      #       # Clone the repository if it doesn't exist
      #       databricks repos create "$REPO_URL" gitHub --path "$REPO_PATH"
      #       echo "✅ Repository cloned successfully"
      #     fi
      - name: Update git folder
        # Set your workspace path and branch name here
        run: databricks repos update $REPO_PATH --branch main